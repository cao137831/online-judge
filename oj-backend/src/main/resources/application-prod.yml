server:
  port: 8101
spring:
  # 数据库配置
  # todo 需替换配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/oj
    username: root
    password: root
  # Redis 配置
  # todo 需替换配置
  redis:
    database: 1
    host: 127.0.0.1
    port: 6379
    timeout: 5000
    password:
  # Elasticsearch 配置
  # todo 需替换配置
#  elasticsearch:
#    uris: http://localhost:9200
#    username: root
#    password: 123456
mybatis-plus:
  configuration:
    # 生产环境关闭日志
    log-impl: ''
# 代码沙箱配置
codesandbox:
  type: remote # example：示例，remote：远程，thirdParty：第三方
  host: 124.70.69.159
  port: 8090